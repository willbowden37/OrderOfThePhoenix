import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    basicText: {
        color: 'blue',
        fontSize: 20,
        alignItems: 'left',
    },
    pageTitle: {
        color: 'blue',
        fontSize: 40,
        alignItems: 'center'
    }
})