import React, {Component} from 'react';
import {Text} from 'react-native';

export default class WinScreen extends Component {

    render() {
        return(
            <Text>This is the Win</Text>
        );
    }
}